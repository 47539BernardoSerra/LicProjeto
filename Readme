TODO:
Fazer a classe M(Out Of Service)//On it
Fazer os users
Fazer o File Access
Fazer o Key Receiver


TOFIX:
Log não usa File Access
Tirar as funções javardonas do KBD


KNOWN BUGS:

Classe M DEV:

KBD tem, publicos, getKey() e waitKey(), separar o getKey em 2 metodos privados, getKeyParallel() GetKeySerial()(Chama as merdas do key receiver
getKey passa a chamar o parallel se constante for false e chama o outro se a cosntante serial for true

